{
  "name": "@agent-mail/imap",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun"
  },
  "dependencies": {
    "@agent-mail/core": "workspace:*"
  }
}
