{
  "name": "agent-mail",
  "private": true,
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": "1.3.14"
  },
  "scripts": {
    "check:exact-versions": "bun -e 'const p=JSON.parse(await Bun.file(\"package.json\").text()); const d=Object.entries({...p.dependencies,...p.devDependencies}); const bad=d.filter(([,v])=>!/^\\d+\\.\\d+\\.\\d+(?:-[0-9A-Za-z.-]+)?$/.test(v)); if(bad.length){console.error(\"Non-exact dependency versions:\",bad.map(([n,v])=>n+\"@\"+v).join(\", \")); process.exit(1)}'"
  },
  "dependencies": {
    "@hono/zod-openapi": "1.6.0",
    "hono": "4.13.2",
    "imapflow": "1.7.1",
    "mailparser": "3.9.15",
    "marked": "18.0.9",
    "sanitize-html": "2.17.7",
    "xstate": "5.32.5",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "@types/mailparser": "3.4.6",
    "@types/sanitize-html": "2.16.1",
    "bun": "1.3.14",
    "vite-plus": "0.2.9"
  }
}
